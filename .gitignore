# python
**/*.pyc
__pycache__/
*.egg-info/
.coverage

# editor + system
*.swp
.vscode/
.idea/
.ipynb_checkpoints/
.DS_Store

# build artifacts
build/
dist/

# python venv
.venv/

# local data caches (parquet / per-gauge hourly history)
*.parquet

# inundation pipeline intermediates (DEM, HAND raster, etc.) — large,
# regenerated by inundation/fetch_dem.py + inundation/hand.py
inundation-data/
*.tif

# secrets — never commit API keys
.env
.env.local
*.env.local
secrets.env
